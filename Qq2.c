/*#include <stdio.h>

int main(){
  int A[3],B[3],C[3],D[3];

  scanf("%d %d %d", &B[0], &B[1], &B[2]);
  scanf("%d %d %d", &C[0], &C[1], &C[2]);
  scanf("%d %d %d", &D[0], &D[1], &D[2]);

  for (int i = 0; i<3;i++){
    A[i] = B[i] + C[i] - 5*D[i];
  }

  printf("A = [%d,%d,%d]", A[0], A[1], A[2]);

  return 0;
}*/